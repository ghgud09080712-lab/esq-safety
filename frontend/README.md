# Frontend

The current frontend is the existing `개선제안서_앱버전.html` file at the workspace root.

The backend serves it at:

```text
http://127.0.0.1:4173/app
```

This keeps the existing app behavior intact while giving us a backend entry point for APIs and future frontend refactoring.
