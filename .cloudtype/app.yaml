name: esq-safety
app: node@20
options:
  env:
    - name: NODE_ENV
      value: production
    - name: PORT
      value: "4173"
    - name: DATA_DIR
      value: "/data"
  install: rm -rf node_modules package-lock.json && npm install --omit=dev
  ports: 4173/http
  start: npm start
